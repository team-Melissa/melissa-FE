import AgreementContainer from '@/src/features/agreement/containers/AgreementContainer';

/**
 * @description 약관 동의 페이지 라우터
 */
export default function AgreementRouter() {
  return <AgreementContainer />;
}
